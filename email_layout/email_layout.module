<?php

/**
 * Implements hook_mail().
 */
function email_layout_mail($key, &$message, $params) {
  switch ($key) {
    case 'layout_email':
      $message['subject'] = $params['subject'];
      $message['body'][] = $params['body'];
      $message['headers']['Content-Type'] = 'text/html; charset=UTF-8';
      break;
  }
}
