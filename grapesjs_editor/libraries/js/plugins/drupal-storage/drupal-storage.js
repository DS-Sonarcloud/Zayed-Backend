/**
 * @file
 * Contains drupal-storage.js 
 */
(function(window, $) {
  window['drupal-storage'] = (editor, opts = {}) => {
    const sm = editor.StorageManager;
    console.log('Drupal Storage plugin initialized');
  };
})(window, jQuery);
