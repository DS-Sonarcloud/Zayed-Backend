{# AEGov Design System — Badge Component #}
<span class="aegov-badge aegov-badge--{{ data.variant|default('primary') }} aegov-badge--{{ data.size|default('md') }}{% if data.pill %} aegov-badge--pill{% endif %}">{{ data.text }}</span>
